"use strtict"; // treat all js code as newer version

//alert(3+3) // we are using node not browser 

let name="Ankit"
let age=21
let isloggedIn =false
let state;

console.log(typeof undefined)//undefined is a datatype 
console.log(typeof null) // null is treated as a
